---
description: 変更をコミットする
argument-hint: [メッセージ（省略可）]
---

変更をコミットしてください。

- git status で変更を確認
- $ARGUMENTS があればそれをメッセージに、なければ変更内容から自動生成
- $ARGUMENTS がコミットメッセージではなく、前回のコミットメッセージの修正などの指示の場合、それに従う。
- ステージング → コミット
- **`Co-authored-by` などの記述は一切禁止。** 特に、コミットメッセージやPRの生成に使用したツール名を絶対に記載しない。
- 確認は不要